pipeline {
    agent { label 'worker' }
    parameters { 
        string( name: 'goood', defaultValue:'hhhhhhhhhh' , description: 'hellososlos') 
        
    }
    stages {
        stage('Hello') {
            steps {
                echo "Hello World ${params.goood}"
            }
        }
        stage('buid') {
            steps {
                 echo "helloaojfih"
            }
    }
    
    }
}
